namespace MealAssistant
{
    public enum MealType
    {
        Breakfast,
        Lunch,
        Snack,
        Dinner
    }

    public enum MealLocation
    {
        Cook,
        TakeOut,
        EatIn
    }
}